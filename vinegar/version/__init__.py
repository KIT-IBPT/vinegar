"""
Provides the version of the Vinegar distribution.
"""

# Version (as a tuple)
version = (0, 0, 1)

# Version (as a string)
version_string = '.'.join([str(ver_component) for ver_component in version])
